# ============================================================
# 🏠 부동산 분석 플랫폼 - 통합 환경변수 설정 예시
# ============================================================
# 이 파일은 템플릿입니다. 
# 이 파일을 복사하여 .env 이름으로 변경하고, 실제 값을 채워 넣으세요.
# ============================================================

# ------------------------------------------------------------
# 🔧 애플리케이션 기본 설정
# ------------------------------------------------------------
PROJECT_NAME=부동산 데이터 분석 서비스
VERSION=1.0.0
DEBUG=true
ENVIRONMENT=development

# ------------------------------------------------------------
# 🗄️ 데이터베이스 설정 (PostgreSQL + PostGIS)
# ------------------------------------------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=realestate
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Backend에서 사용하는 DB URL (컨테이너 내부 통신용 'db' 호스트 사용)
# 형식: postgresql+asyncpg://<user>:<password>@<host>:<port>/<db_name>
DATABASE_URL=postgresql+asyncpg://postgres:your_secure_password@db:5432/realestate

# ------------------------------------------------------------
# 🔴 Redis 설정 (캐싱)
# ------------------------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379/0

# ------------------------------------------------------------
# 🔐 JWT 인증 설정
# ------------------------------------------------------------
# ⚠️ 중요: 프로덕션에서는 반드시 강력한 랜덤 키로 변경하세요!
SECRET_KEY=your-super-secret-key-change-me-in-production

ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
REFRESH_TOKEN_EXPIRE_DAYS=7

# ------------------------------------------------------------
# 🌐 CORS 설정
# ------------------------------------------------------------
# 허용할 프론트엔드 도메인 (콤마로 구분)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8081

# ------------------------------------------------------------
# 🚀 서버 포트 설정
# ------------------------------------------------------------
BACKEND_PORT=8000
FRONTEND_PORT=3000

# ------------------------------------------------------------
# 🏛️ 외부 API 키 - 국토교통부 (공공데이터포털)
# ------------------------------------------------------------
MOLIT_API_KEY=your_molit_api_key

# ------------------------------------------------------------
# 🗺️ 외부 API 키 - 카카오
# ------------------------------------------------------------
# JavaScript API Key: 지도 SDK 로드 시 사용
KAKAO_JAVASCRIPT_KEY=your_kakao_javascript_key
VITE_KAKAO_JAVASCRIPT_KEY=your_kakao_javascript_key

# REST API Key: REST API 호출 시 사용
KAKAO_REST_API_KEY=your_kakao_rest_api_key

# ------------------------------------------------------------
# 📰 외부 API 키 - 뉴스 (선택)
# ------------------------------------------------------------
NAVER_CLIENT_ID=your_naver_client_id
NAVER_CLIENT_SECRET=your_naver_client_secret

# ------------------------------------------------------------
# 🤖 AI API 키 - Google Gemini (P2 기능)
# ------------------------------------------------------------
GEMINI_API_KEY=your_google_gemini_api_key

# ------------------------------------------------------------
# 📧 이메일 설정 (비밀번호 재설정용)
# ------------------------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password

# ------------------------------------------------------------
# 📱 Frontend 설정 (Expo)
# ------------------------------------------------------------
EXPO_PUBLIC_API_URL=http://localhost:8000/api/v1

# ------------------------------------------------------------
# 🔐 Clerk 인증 설정
# ------------------------------------------------------------
CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key
CLERK_SECRET_KEY=your_clerk_secret_key
CLERK_WEBHOOK_SECRET=your_clerk_webhook_secret
